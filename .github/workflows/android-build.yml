name: Build Android APK

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Build web application
      run: npm run build

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Setup Android SDK
      uses: android-actions/setup-android@v3

    - name: Sync Capacitor
      run: npx cap sync android

    - name: Grant execute permission for gradlew
      run: chmod +x android/gradlew

    - name: Build debug APK
      working-directory: ./android
      run: ./gradlew assembleDebug

    - name: Upload debug APK
      uses: actions/upload-artifact@v4
      with:
        name: verum-omnis-debug
        path: android/app/build/outputs/apk/debug/app-debug.apk
        retention-days: 30

  build-release:
    runs-on: ubuntu-latest
    # Only build release on main branch or manual dispatch
    if: github.ref == 'refs/heads/main' || github.event_name == 'workflow_dispatch'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    # ========================================
    # CI GUARD: Prevent signing regressions
    # DO NOT MODIFY WITHOUT SECURITY REVIEW
    # ========================================
    - name: CI Guard - Check for masked placeholders in workflow
      run: |
        echo "Checking for masked signing placeholders in workflow..."
        if grep -q '\*\*\*\*\*\*' .github/workflows/android-build.yml; then
          echo "ERROR: Masked signing placeholder (******) detected in android-build.yml"
          echo "This indicates a potential security regression where secrets were committed."
          exit 1
        fi
        echo "✓ No masked placeholders found"

    - name: CI Guard - Check for SHA-256 signing references
      run: |
        echo "Checking for SHA-256 signing references in workflow..."
        if grep -iE 'sha-?256.*sign' .github/workflows/android-build.yml; then
          echo "ERROR: SHA-256 signing detected in android-build.yml"
          echo "APK signing must rely on keystore only, not SHA-256 hash signing."
          exit 1
        fi
        echo "✓ No SHA-256 signing references found"

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Build web application
      run: npm run build

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Setup Android SDK
      uses: android-actions/setup-android@v3

    - name: Sync Capacitor
      run: npx cap sync android

    - name: Grant execute permission for gradlew
      run: chmod +x android/gradlew

    # ========================================
    # SIGNING STEPS
    # DO NOT MODIFY WITHOUT SECURITY REVIEW
    # ========================================
    
    # Validate signing secrets exist on main branch
    - name: Validate signing secrets on main branch
      if: github.ref == 'refs/heads/main'
      run: |
        if [ -z "${{ secrets.KEYSTORE_BASE64 }}" ] || \
           [ -z "${{ secrets.KEYSTORE_PASSWORD }}" ] || \
           [ -z "${{ secrets.KEY_ALIAS }}" ] || \
           [ -z "${{ secrets.KEY_PASSWORD }}" ]; then
          echo "❌ ERROR: Signing secrets are required for main branch builds"
          echo "Please configure the following GitHub secrets:"
          echo "  - KEYSTORE_BASE64"
          echo "  - KEYSTORE_PASSWORD"
          echo "  - KEY_ALIAS"
          echo "  - KEY_PASSWORD"
          echo ""
          echo "See SIGNING_SETUP.md for instructions."
          exit 1
        fi
        echo "✓ All signing secrets are configured"
    
    # Decode keystore from secrets
    - name: Decode Keystore
      if: ${{ secrets.KEYSTORE_BASE64 }}
      run: |
        echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > android/app/keystore.jks

    # Build signed release APK
    - name: Build signed release APK
      if: ${{ secrets.KEYSTORE_BASE64 }}
      working-directory: ./android
      env:
        KEYSTORE_FILE: app/keystore.jks
        KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
        KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
        KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        REQUIRE_SIGNED_RELEASE: "true"
      run: |
        ./gradlew assembleRelease \
          -Pandroid.injected.signing.store.file=app/keystore.jks \
          -Pandroid.injected.signing.store.password=$KEYSTORE_PASSWORD \
          -Pandroid.injected.signing.key.alias=$KEY_ALIAS \
          -Pandroid.injected.signing.key.password=$KEY_PASSWORD

    # Verify APK signature (CRITICAL - ensures APK is actually signed)
    - name: Verify APK signature with apksigner
      if: ${{ secrets.KEYSTORE_BASE64 }}
      run: |
        # Find the latest apksigner
        APKSIGNER=$(find $ANDROID_HOME/build-tools -name apksigner | sort -V | tail -n 1)
        if [ -z "$APKSIGNER" ]; then
          echo "❌ apksigner not found in Android SDK"
          exit 1
        fi
        echo "Using apksigner: $APKSIGNER"
        echo ""
        echo "=== APK Signature Verification ==="
        
        # Run verification and check result directly
        if $APKSIGNER verify --print-certs android/app/build/outputs/apk/release/app-release.apk; then
          echo ""
          echo "✓ APK signature verified successfully"
          echo "✓ APK is cryptographically signed and ready for distribution"
        else
          echo ""
          echo "❌ APK signature verification FAILED"
          echo "❌ APK is NOT properly signed"
          exit 1
        fi

    # Build unsigned release if no keystore (NON-MAIN BRANCHES ONLY)
    - name: Build unsigned release APK (development only)
      if: ${{ !secrets.KEYSTORE_BASE64 && github.ref != 'refs/heads/main' }}
      working-directory: ./android
      run: |
        echo "⚠️  WARNING: Building unsigned APK (development/testing only)"
        echo "⚠️  This APK cannot be distributed to end users"
        ./gradlew assembleRelease
    
    # Final verification: Ensure APK is signed on main branch
    - name: Final verification - Ensure APK is signed on main
      if: github.ref == 'refs/heads/main'
      run: |
        echo "=== Final APK Signature Check ==="
        # Find the latest apksigner
        APKSIGNER=$(find $ANDROID_HOME/build-tools -name apksigner | sort -V | tail -n 1)
        
        # Run verification silently and check result
        if $APKSIGNER verify android/app/build/outputs/apk/release/app-release.apk >/dev/null 2>&1; then
          echo "✓ Final verification passed: APK is cryptographically signed"
          echo "✓ APK is ready for Google Play and enterprise distribution"
        else
          echo "❌ CRITICAL FAILURE: APK on main branch is NOT signed"
          echo "❌ This violates security requirements"
          echo "❌ Unsigned APKs cannot be distributed to production"
          exit 1
        fi
    
    # ========================================
    # END SIGNING STEPS
    # ========================================

    # Verify release APK exists
    - name: Verify release APK exists
      run: |
        if [ ! -f "android/app/build/outputs/apk/release/app-release.apk" ]; then
          echo "ERROR: Expected release APK not found at android/app/build/outputs/apk/release/app-release.apk"
          echo "Build may have failed silently or output path changed."
          ls -la android/app/build/outputs/apk/release/ || echo "Release directory does not exist"
          exit 1
        fi
        echo "✓ Release APK verified at android/app/build/outputs/apk/release/app-release.apk"

    # Print build information
    - name: Print build information
      working-directory: ./android
      run: |
        echo "=== Android Build Information ==="
        echo "Application ID: $(./gradlew -q printApplicationId 2>/dev/null || grep 'applicationId' app/build.gradle | awk '{print $2}' | tr -d '"')"
        echo "Version Name: $(grep 'versionName' app/build.gradle | awk '{print $2}' | tr -d '"')"
        echo "Version Code: $(grep 'versionCode' app/build.gradle | awk '{print $2}')"
        echo "================================="

    - name: Confirm release APK
      run: |
        ls -lh android/app/build/outputs/apk/release/

    - name: Upload release APK
      uses: actions/upload-artifact@v4
      with:
        name: verum-omnis-release
        path: android/app/build/outputs/apk/release/*.apk
        retention-days: 90

    - name: Create Release
      if: github.ref == 'refs/heads/main' && github.event_name == 'push'
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v${{ github.run_number }}
        name: Release v${{ github.run_number }}
        files: android/app/build/outputs/apk/release/*.apk
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
